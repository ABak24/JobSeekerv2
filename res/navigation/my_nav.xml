<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/posts">

    <fragment
        android:id="@+id/posts"
        android:name="com.example.jobseeker.WorkerUI.Posts"
        android:label="Posts"
        tools:layout="@layout/fragment_posts" />
    <fragment
        android:id="@+id/saved_posts"
        android:name="com.example.jobseeker.WorkerUI.Saved_posts"
        android:label="Saved posts"
        tools:layout="@layout/fragment_saved_posts" />
    <fragment
        android:id="@+id/profile"
        android:name="com.example.jobseeker.WorkerUI.Profile"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />
</navigation>